# 🚀 دليل تشغيل المشروع - Mini Chatbot

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Python 3.8 أو أحدث** - [تحميل Python](https://www.python.org/downloads/)
2. **Node.js 16 أو أحدث** - [تحميل Node.js](https://nodejs.org/)
3. **WampServer64** - يجب أن يكون مثبتًا ومشغلًا
4. **MySQL** - يأتي مع WampServer64

## 🔧 الخطوة 1: إعداد قاعدة البيانات

### 1.1 تشغيل WampServer64
- تأكد من أن WampServer64 يعمل (الأيقونة خضراء في شريط المهام)
- تأكد من أن خدمة MySQL نشطة

### 1.2 إنشاء قاعدة البيانات

1. افتح **phpMyAdmin**:
   - انقر على أيقونة WampServer في شريط المهام
   - اختر "phpMyAdmin"
   - أو افتح المتصفح على: `http://localhost/phpmyadmin`

2. أنشئ قاعدة البيانات:
   - انقر على تبويب "قواعد البيانات" (Databases)
   - في حقل "إنشاء قاعدة بيانات" أدخل: `mini-chat-python`
   - اختر الترتيب: `utf8mb4_unicode_ci`
   - انقر على "إنشاء" (Create)

✅ **تم!** قاعدة البيانات جاهزة الآن.

## 📦 الخطوة 2: تثبيت المتطلبات

### 2.1 تثبيت متطلبات Backend (Python)

افتح **PowerShell** أو **Command Prompt** وانتقل إلى مجلد المشروع:

```bash
cd C:\Users\lapto\Downloads\mini-chat-python\backend
```

أنشئ بيئة افتراضية (Virtual Environment):

```bash
python -m venv venv
```

فعّل البيئة الافتراضية:

```bash
venv\Scripts\activate
```

ستلاحظ أن `(venv)` ظهر في بداية السطر.

الآن ثبّت المكتبات المطلوبة:

```bash
pip install -r requirements.txt
```

⏳ انتظر حتى يكتمل التثبيت (قد يستغرق بضع دقائق).

### 2.2 تثبيت متطلبات Frontend (React)

افتح **PowerShell جديد** وانتقل إلى مجلد Frontend:

```bash
cd C:\Users\lapto\Downloads\mini-chat-python\frontend
```

ثبّت المكتبات:

```bash
npm install
```

⏳ انتظر حتى يكتمل التثبيت.

## 🚀 الخطوة 3: تشغيل المشروع

### 3.1 تشغيل Backend (الخادم)

في **Terminal الأول** (PowerShell):

```bash
cd C:\Users\lapto\Downloads\mini-chat-python\backend
venv\Scripts\activate
python main.py
```

✅ يجب أن ترى رسالة:
```
✅ Gemini API client initialized successfully
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**لا تغلق هذا النافذة!** اتركها مفتوحة.

### 3.2 تشغيل Frontend (واجهة المستخدم)

في **Terminal الثاني** (PowerShell جديد):

```bash
cd C:\Users\lapto\Downloads\mini-chat-python\frontend
npm start
```

✅ يجب أن يفتح المتصفح تلقائيًا على: `http://localhost:3000`

إذا لم يفتح تلقائيًا، افتح المتصفح يدويًا واذهب إلى: `http://localhost:3000`

## 🎉 الخطوة 4: استخدام التطبيق

1. **إنشاء محادثة جديدة**:
   - انقر على زر "Nouvelle discussion" في الشريط الجانبي

2. **إرسال رسالة**:
   - اكتب رسالتك في حقل الإدخال السفلي
   - يمكنك الكتابة بالعربية، الفرنسية، أو الإنجليزية
   - اضغط Enter أو انقر على زر الإرسال

3. **رفع صورة**:
   - انقر على أيقونة الصورة 📷
   - اختر صورة من جهازك
   - اكتب سؤالًا عن الصورة
   - أرسل الرسالة

4. **إدارة المحادثات**:
   - انقر على أي محادثة في الشريط الجانبي لفتحها
   - انقر على ✏️ لإعادة تسمية المحادثة
   - انقر على 🗑️ لحذف المحادثة

## 🔍 التحقق من أن كل شيء يعمل

### ✅ تحقق من Backend:
- Terminal الأول يعرض: `Uvicorn running on http://0.0.0.0:8000`
- لا توجد أخطاء حمراء في Terminal

### ✅ تحقق من Frontend:
- المتصفح مفتوح على `http://localhost:3000`
- ترى واجهة التطبيق مع الشريط الجانبي

### ✅ تحقق من قاعدة البيانات:
- افتح phpMyAdmin: `http://localhost/phpmyadmin`
- انقر على قاعدة البيانات `mini-chat-python`
- بعد إرسال رسالة، يجب أن ترى جدولين:
  - `conversations` - المحادثات
  - `messages` - الرسائل

## 🐛 حل المشاكل الشائعة

### ❌ خطأ: "Can't connect to MySQL server"

**الحل**:
1. تأكد من أن WampServer64 يعمل
2. تأكد من أن MySQL نشط (الأيقونة خضراء)
3. أعد تشغيل WampServer64 إذا لزم الأمر

### ❌ خطأ: "Unknown database 'mini-chat-python'"

**الحل**:
1. افتح phpMyAdmin
2. أنشئ قاعدة البيانات يدويًا باسم `mini-chat-python`
3. تأكد من الترتيب: `utf8mb4_unicode_ci`

### ❌ خطأ: "Gemini API client not initialized"

**الحل**:
1. تأكد من وجود ملف `.env` في مجلد `backend`
2. تأكد من أن الملف يحتوي على:
   ```
   GOOGLE_API_KEY=AIzaSyCVRXxDvQZfH-BUVYSLGUFpRwdJXjKHtJg
   ```

### ❌ خطأ: "Port 8000 already in use"

**الحل**:
1. أغلق البرنامج الآخر الذي يستخدم المنفذ 8000
2. أو غيّر المنفذ في `backend/main.py`:
   ```python
   uvicorn.run(app, host="0.0.0.0", port=8001)  # غيّر 8000 إلى 8001
   ```

### ❌ خطأ: "npm: command not found"

**الحل**:
1. تأكد من تثبيت Node.js
2. أعد تشغيل Terminal بعد التثبيت
3. تحقق من التثبيت: `node --version`

### ❌ خطأ: "pip: command not found"

**الحل**:
1. تأكد من تثبيت Python
2. تأكد من إضافة Python إلى PATH أثناء التثبيت
3. أعد تشغيل Terminal

## 📝 ملاحظات مهمة

- **يجب أن يكون Backend و Frontend يعملان في نفس الوقت**
- **لا تغلق Terminal Backend** - يجب أن يبقى مفتوحًا
- **قاعدة البيانات تُنشأ تلقائيًا** عند أول تشغيل للـ Backend
- **الصور المرفوعة** تُحفظ في مجلد `backend/uploads/`
- **المحادثات** تُحفظ تلقائيًا في قاعدة البيانات

## 🎯 استخدام سريع (Scripts)

يمكنك استخدام الملفات الجاهزة:

### تشغيل Backend:
- انقر نقرًا مزدوجًا على: `start_backend.bat`

### تشغيل Frontend:
- انقر نقرًا مزدوجًا على: `start_frontend.bat`

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل" أعلاه
2. تحقق من ملفات السجلات في Terminal
3. تأكد من تثبيت جميع المتطلبات بشكل صحيح

---

**تم التهيئة بنجاح! استمتع باستخدام Mini Chatbot! 🎉**
